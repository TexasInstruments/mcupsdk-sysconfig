%%{
    let common = system.getScript("/common");
    let module = system.modules['/board/pmic/pmic'];
%%}
% let instance = module.$instances[0];
% let config = module.getInstanceConfig(instance);
% if (config.device == "BLACKBIRD") {
    Drivers_pmicOpen();
% }
