%%{
    let common = system.getScript("/common");
    let filex_module = system.modules['/fs/filex/filex'];
%%}
/*
 * FileX
 */

#include <fx_api.h>

extern FX_MEDIA gMedia[];

int32_t EclipseThreadx_FilexOpen(void);
void EclipseThreadx_FilexClose(void);